const PATHS = {
    // Public
    HOME: "/",
    LOGIN: "/login",
    REGISTER: "/register",

    // Dashboards
    DASHBOARD: "/dashboard",

    // Errors
    NOT_FOUND: "*",
    UNAUTHORIZED: "/unauthorized",
};

export default PATHS;